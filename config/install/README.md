# Installation Configuration

This directory contains the hand-written install bundle for the operator.

Current manifests:
- `operator-install.yaml` as a one-shot install bundle for CRDs, RBAC, namespace, and manager deployment
